# Requirements for streamlit_app.py -- this is what Streamlit Community
# Cloud installs to run the dashboard. Kept minimal on purpose so deploys
# stay fast and reliable.
streamlit>=1.38.0
pandas>=2.0.0
altair>=5.0.0

# Pipeline dependencies (fetching, tagging, transcribing, optional LLM
# enrichment) live in pipeline/requirements.txt instead -- the dashboard
# never imports them, so they shouldn't slow down or risk the Cloud deploy.
