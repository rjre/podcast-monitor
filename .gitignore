__pycache__/
*.pyc
.DS_Store
.env

# data/transcripts/ (full transcript text) is committed -- this repo is
# private, so the earlier copyright concern about public redistribution
# doesn't apply. See data/transcripts/README.md.

# Working file for pipeline/manual_review.py -- staging state for whichever
# batch is currently being reviewed, not meaningful once applied.
data/manual_review/

